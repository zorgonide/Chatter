const defaultState = {
    token: null,
    user: {}
}

const auth = (state = defaultState, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default auth;